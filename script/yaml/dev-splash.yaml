# run: flutter pub run flutter_native_splash:create --flavor dev --path=dev-splash.yaml

flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/splash-dev.png"
  android_12:
    image: "assets/images/splash-dev.png"
  ios: true